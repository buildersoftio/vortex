namespace Vortex.Core.Models.Common.Clusters
{
    public enum NodeStatus
    {
        Online,
        Offline
    }

    public enum NodeState
    {
        Follower,
        Candidate,
        Leader
    }
}
